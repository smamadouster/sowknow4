FROM node:20-alpine

WORKDIR /app

# Accept build-time env vars so Next.js Edge Middleware can inline them
ARG JWT_SECRET
ARG NEXT_PUBLIC_API_URL
ENV JWT_SECRET=${JWT_SECRET}
ENV NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}

COPY package*.json ./

RUN npm install

CMD ["npm", "run", "dev"]
